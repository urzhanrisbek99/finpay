-- Стартовый баланс нового аккаунта — 0.
--
-- В 0006 дефолт был выставлен в 1240500: число из демо-данных, оно попадало
-- каждому, кто регистрируется, — новый юзер сразу видел чужие деньги.
-- Пополнение идёт через add_income, поэтому пустой старт — единственный
-- корректный вариант.
--
-- Выполнить в SQL Editor целиком.

alter table public.profiles alter column balance set default 0;
